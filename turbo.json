{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "prisma:generate": {},
    "build": {
      "outputs": [
        "dist/**"
      ],
      "dependsOn": [
        "prisma:generate",
        "^build"
      ]
    },
    "test": {
      "dependsOn": [
        "build"
      ],
      "inputs": [
        "src/**/*.ts",
        "test/**/*.ts"
      ]
    },
    "format": {},
    "lint": {}
  }
}
